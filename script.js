// Optional: You can add interactive features here later
console.log("Portfolio loaded for Jane Doe.");
function toggleProject(element) {
  const card = element.parentElement;
  card.classList.toggle("active");
}